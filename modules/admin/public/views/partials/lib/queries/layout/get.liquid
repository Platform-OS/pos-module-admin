{% liquid
  function admin_layout_implementations = 'modules/core/commands/hook/fire', hook: 'admin_layout', merge_to_object: null
  if admin_layout_implementations.first != blank
    return admin_layout_implementations.first
  endif
  return 'modules/admin/layouts/default'
%}
